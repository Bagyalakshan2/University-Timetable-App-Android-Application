<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.LoginApplication">

        <!-- Course selection activity -->
        <activity android:name=".Fulltimetable" />
        <activity android:name=".NetOrSoft" />
        <activity android:name=".Network" />
        <activity android:name=".Software" />

        <activity
            android:name=".SelectCourseActivity"
            android:exported="false" />

        <!-- Main activity (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
